apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: oficina-mecanica

resources:
  - ../../base

# Produção usa os defaults da base (NODE_ENV=production, LOG_LEVEL=info).
# Nas próximas issues, este overlay recebe:
#   - a tag da imagem da API, injetada pelo cd.yml via `kustomize edit set image` (DEV-65);
#   - o Secret sincronizado do AWS Secrets Manager pelo External Secrets Operator (DEV-62),
#     substituindo o template `secret.yaml` da base.
