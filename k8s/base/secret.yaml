# TEMPLATE / EXEMPLO — não contém segredos reais e NÃO deve ser aplicado como está.
#
# Em produção este Secret NÃO é versionado nem aplicado manualmente: os valores
# vêm do AWS Secrets Manager e são sincronizados para o cluster pelo External
# Secrets Operator (ver DEV-62). Mantemos este arquivo apenas para documentar as
# chaves esperadas pela aplicação e permitir um deploy local/dev.
apiVersion: v1
kind: Secret
metadata:
  name: oficina-api-secret
  labels:
    app.kubernetes.io/name: oficina-mecanica
    app.kubernetes.io/component: api
type: Opaque
stringData:
  DATABASE_URL: "postgresql://usuario:senha@HOST_DO_RDS:5432/oficina?schema=public"
  JWT_SECRET: "troque-por-um-segredo-forte-e-aleatorio"
